# Glyzier Backend Environment Variables
# =====================================
# Copy this file to .env (gitignored) or set as environment variables
# For local development, configure in application-supabase.properties instead

# Email Configuration (Gmail SMTP)
# ---------------------------------
# Used for sending password reset codes
# Generate Gmail App Password at: https://myaccount.google.com/apppasswords
EMAIL_USERNAME=your-email@gmail.com
EMAIL_PASSWORD=your-16-character-app-password
EMAIL_FROM=your-email@gmail.com

# Database Configuration (Supabase PostgreSQL)
# --------------------------------------------
# Get these from: Supabase Dashboard → Project Settings → Database
DB_URL=jdbc:postgresql://YOUR_SUPABASE_POOLER_HOST:5432/postgres
DB_USERNAME=postgres.YOUR_PROJECT_ID
DB_PASSWORD=YOUR_DATABASE_PASSWORD

# JWT Configuration
# -----------------
# Use a strong, random secret key in production (minimum 256 bits)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRATION=86400000

# Supabase Storage Configuration
# ------------------------------
# Get these from: Supabase Dashboard → Project Settings → API
SUPABASE_URL=https://YOUR_PROJECT_ID.supabase.co
SUPABASE_KEY=YOUR_SUPABASE_ANON_KEY

# Server Configuration
# --------------------
SERVER_PORT=8080
SPRING_PROFILES_ACTIVE=supabase

# Note: For local development, use application-supabase.properties instead
# For production/deployment, set these as environment variables in your hosting platform
